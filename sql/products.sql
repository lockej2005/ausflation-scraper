CREATE TABLE products (
    id text,
    title text,
    price_value numeric,
    was_price_value numeric,
    save_value numeric,
    unit_price text,
    url text,
    image_url text,
    category text,
    page integer,
    scrapedAt text,
    uploaded_at text,
    vendor text
);
